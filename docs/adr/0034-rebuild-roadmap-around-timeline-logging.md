# Rebuild the Roadmap Around Timeline Logging

The learning roadmap is rebuilt around the Timeline Logging domain model instead of the original StudyTrack application. The roadmap now treats CLI, plain PHP web, Laravel, Laravel API, and Next.js as separate implementations of the same Log Entry and Context model, with categories, archiving, read models, queues, APIs, and frontend integration introduced when they create clear learning value.
