# Defer Archiving After the CLI Phase

Archived Log Entry remains part of the domain model, but the initial CLI application will not implement archiving. The first CLI should focus on placing records on the timeline, assigning context, and adding end times; archiving becomes more useful once review and filtering workflows exist.
