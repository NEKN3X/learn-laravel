# Use Context for Branch-Like Streams

The domain model will include Context as the branch-like stream that a Log Entry belongs to, such as work, private, a client project, or a learning project. The product may use a commit-history metaphor internally, but the user-facing domain term is Context rather than Branch, and categories remain separate labels for meaning within a context.
