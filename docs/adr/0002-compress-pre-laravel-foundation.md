# Compress the Pre-Laravel PHP Foundation

The roadmap will combine the pre-Laravel PHP work into a smaller set of application-building phases instead of treating every language/tool milestone as a separate project phase. This preserves the value of learning raw PHP, file persistence, OOP, Composer, PDO, tests, and plain HTTP while moving to Laravel early enough for the main goal: practical Laravel web application skill.
