# Start CLI with In-Memory Storage

Status: superseded by ADR-0035

The first CLI application will start with in-memory storage only. This keeps the initial learning step focused on PHP basics, command parsing, arrays, functions, and the shape of Log Entry and Context operations before adding JSON, SQLite, or framework persistence.
