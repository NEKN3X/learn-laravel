# Use User-Defined Categories

Activity categories and quick note categories will be user-defined rather than fixed by the application. This lets the tracking model adapt to each user's actual life and work, while keeping activity categories and quick note categories as separate category systems.
