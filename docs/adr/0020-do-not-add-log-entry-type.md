# Do Not Add Log Entry Type

Log Entry will not have a built-in type such as activity or note. The model treats all records as timeline entries with text, recorded time, optional end time, and optional user-defined categories, avoiding a hard split between actions, thoughts, observations, and learned details.
