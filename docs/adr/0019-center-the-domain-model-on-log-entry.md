# Center the Domain Model on Log Entry

The domain model is now centered on Log Entry rather than separate Activity Log and Quick Note concepts. A Log Entry starts as a point on the timeline and becomes readable as a span when an end time is added, allowing actions, thoughts, observations, and learned details to share one model without requiring separate activity and note workflows.
