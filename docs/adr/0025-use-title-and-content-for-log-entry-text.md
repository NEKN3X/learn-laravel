# Use Title and Content for Log Entry Text

Log Entry will use a required `title` and optional `content` instead of a single text field. This follows a commit-message-like shape: the title supports scanning the timeline, while content can hold details when a record needs more context.
