# Allow Context Override per Log Entry

Current Context is a default, not a requirement. A user may override the context for a single Log Entry, or record it without a context, so that a thought from another work or life stream can be captured without switching away from the current context.
