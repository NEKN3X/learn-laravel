# Keep Current Context

The application will remember a Current Context as the default destination for new Log Entries. This follows the branch-like workflow: users switch context once, then continue recording entries into that stream until they switch again or clear the current context.
