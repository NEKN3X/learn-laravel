# Allow Multiple Log Categories

Log Entries may have multiple user-defined Log Categories. Because Log Entry has no built-in type, categories provide flexible meaning without forcing a record into a single application-defined classification.
