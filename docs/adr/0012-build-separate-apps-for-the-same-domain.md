# Build Separate Apps from the Domain Model

The roadmap will implement the activity tracking domain model as separate learning applications instead of evolving one codebase from CLI to Laravel to Next.js. This keeps each app focused on the skills it is meant to teach while preserving domain language through `CONTEXT.md` and ADRs.
