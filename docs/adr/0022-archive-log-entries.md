# Archive Log Entries

Any Log Entry may be archived with an `archived_at` timestamp. Archiving is a lightweight way to remove entries from active review without deleting them, and it applies consistently now that activity-like and note-like records share the Log Entry model.
