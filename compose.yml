services:
  php:
    image: php:8.5-cli
    working_dir: /var/www/html
    environment:
      TZ: Asia/Tokyo
    ports:
      - "8000:8000"
    volumes:
      - .:/var/www/html
    command: php -S 0.0.0.0:8000 -t public
